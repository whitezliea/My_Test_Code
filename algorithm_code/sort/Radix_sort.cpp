#include<iostream>
#include<stdio.h>
#include<stdlib.h>
#define MAXD 3
using namespace std;
/*
typedef struct node
{
    char data[MAXD]; //MAXD 100 --> 3
    struct node * next;
}NodeType;

void RadixSort(NodeType * &N,int r ,int d) // 100 --> r=10,d=3
{
    NodeType *head[MAXD], *tail[MAXD],*t;
    int i,j,k;
    for(i=0;i<d-1;i++)  //low-->high
    {
        for(j=0;j<r;j++)
        {
            head[j] = tail[j] =NULL;  //init
        }

        while (N!=NULL)
        {
            k= N->data[i]-'0';

        }
    }
}
*/